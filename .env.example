# Local development — gitignored. Rotate keys if this file was ever committed.

NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_PUBLIC_APP_URL=https://traveltourup-next1.vercel.app

# Canonical origin for sitemaps, canonical URLs, and Open Graph (production: https://yourdomain.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional: server-only alias read by getSiteUrl() if NEXT_PUBLIC_* unset
SITE_URL=http://localhost:3000
# <meta name="google-site-verification" content="5qDuxgLKGIcHBkQsNmKkZ97eIlS5jB9NTZA8QAFi4Gw" />
# Search / social (optional)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=5qDuxgLKGIcHBkQsNmKkZ97eIlS5jB9NTZA8QAFi4Gw
NEXT_PUBLIC_BING_SITE_VERIFICATION=
# Optional ISO 8601 date for sitemap lastmod of static marketing URLs (bump when public SEO copy changes materially)
NEXT_PUBLIC_SEO_STATIC_LASTMOD=2026-05-08T00:00:00.000Z
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_TWITTER_SITE=traveltourup
NEXT_PUBLIC_SOCIAL_TWITTER_URL=https://twitter.com/traveltourup
NEXT_PUBLIC_SOCIAL_FACEBOOK_URL=https://facebook.com/traveltourup
NEXT_PUBLIC_SOCIAL_INSTAGRAM_URL=https://instagram.com/traveltourup
NEXT_PUBLIC_SOCIAL_LINKEDIN_URL=https://linkedin.com/company/traveltourup

NEXT_PUBLIC_SUPABASE_URL=https://kendoknarzifeviokock.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtlbmRva25hcnppZmV2aW9rb2NrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQzNjkzMjYsImV4cCI6MjA4OTk0NTMyNn0.906CNlHPJ13xES5mSMk9mC17aIXIKkTNTug3w4tC31o

# Add from Supabase Dashboard → Settings → API → service_role (optional until admin/server ops need it)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtlbmRva25hcnppZmV2aW9rb2NrIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NDM2OTMyNiwiZXhwIjoyMDg5OTQ1MzI2fQ.x41Y7iu_rZDXggebXhl7b1xRu7qzLTdlPARcPVPZArQ

DATABASE_URL="postgresql://postgres.kendoknarzifeviokock:SpellLink%40123@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require"
# Migrations: direct host (see Supabase → Settings → Database → connection string “URI”)
DIRECT_URL="postgresql://postgres:SpellLink%40123@db.kendoknarzifeviokock.supabase.co:5432/postgres?sslmode=require"

DUFFEL_API_KEY=duffel_test_REPLACE_ME
DUFFEL_API_URL=https://api.duffel.com
# Note: DUFFEL_API_KEY is server-only and never exposed to the client.
# The backend uses it to generate temporary component client keys for secure card tokenization.
# Component client keys are dynamically generated per-session via /api/duffel/client-key



SUPER_ADMIN_EMAIL=superadmin@admin.com
SUPER_ADMIN_PASSWORD=SpellLink@123



# From Duffel Dashboard when creating a webhook (required for POST /api/v1/webhooks/duffel)
DUFFEL_WEBHOOK_SECRET=

# Flight checkout — Duffel PaymentIntent gross-up (see Duffel “Collecting customer card payments”).
FLIGHT_COMMISSION_PERCENT=5
FLIGHT_MARKUP_FIXED=0
# Assumed card fee for (fare+markup)/customer currency; domestic vs intl may differ — tune per Duffel dashboard.
DUFFEL_PAYMENTS_FEE_RATE=0.029
# 1 when customer pays in same currency as offer; otherwise mid-market × small buffer per Duffel guide.
FLIGHT_PAYMENT_FX_RATE=1
# Max drift between PaymentIntent snapshot and refreshed offer total (major units) before order.
FLIGHT_PRICE_TOLERANCE_MAJOR=2
# Default flight PSP (only duffel_payments implemented).
FLIGHT_PAYMENT_PROVIDER=duffel_payments

# Shared secret for internal ops cron handlers (orphan PIT sweeper, refund poller, cancellation quote
# expirer). Required in production. Generate with: openssl rand -hex 32
# Caller MUST send: Authorization: Bearer <OPS_JOB_TOKEN>
OPS_JOB_TOKEN=
# When OPS_JOB_TOKEN is empty AND NODE_ENV !== production, set this to "1" to allow unauthenticated
# local invocation (e.g. for testing crons via curl).
OPS_JOB_TOKEN_DEV_BYPASS=

# Server log level: debug | info | warn | error. Default: info in production, debug otherwise.
LOG_LEVEL=

RESEND_API_KEY=re_R29zMKVp_Li98jhGeeFCQjmTWNwsL9Jit
# Resend rejects Gmail as "From" until you verify a domain. Use their sandbox sender for dev, or no-reply@your-verified-domain.com in production.
EMAIL_FROM=info@traveltourup.com

# Machine translation (admin Auto Translate). Managed https://libretranslate.com requires LIBRE_TRANSLATE_API_KEY.
# With an empty key, the app uses LIBRE_TRANSLATE_STAGING_URL (default public mirror) for local/staging.
LIBRE_TRANSLATE_URL=https://libretranslate.com
LIBRE_TRANSLATE_API_KEY=
# LIBRE_TRANSLATE_STAGING_URL=https://translate.fedilab.app
# TRANSLATION_REQUEST_TIMEOUT_MS=120000
